# Web框架
fastapi==0.121.0
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# 数据库
sqlalchemy==2.0.25
asyncpg==0.29.0    # PostgreSQL异步驱动
psycopg2-binary==2.9.9  # PostgreSQL同步驱动（用于迁移脚本）
alembic==1.14.0  # 数据库迁移工具
aiosqlite==0.22.1

# 数据验证
pydantic==2.12.4
pydantic-settings==2.11.0

# AI服务
openai==2.7.0
anthropic==0.72.0

# 工具库
httpx==0.28.1
python-dotenv==1.1.0
psutil==6.1.1
aiosmtplib==4.0.2
prometheus-client==0.21.1  # Prometheus /metrics 指标暴露
# MCP官方库（Model Context Protocol Python SDK）
mcp==1.22.0

# 认证与安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==50.0.1  # Fernet 敏感字段加密（settings 表 api_key/cover_api_key/smtp_password）
structlog==24.4.0

# 宽松JSON解析
json5==0.12.1

# orjson — 高性能JSON（比标准库快3-10倍）
orjson==3.11.9

# 重依赖（向量库/ML/NLP/可视化）已移至 requirements-full.txt
# 仅 PM Agent 核心功能无需安装这些包

# 测试工具
pytest>=7.4,<9
pytest-asyncio>=0.21,<1
pytest-xdist>=3.5,<4
redis>=5.0,<6

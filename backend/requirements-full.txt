# 可选依赖 — OOC检测/向量记忆/可视化/统计分析
# 安装方式: pip install -r requirements-full.txt
# 这些依赖体积较大，非核心功能所需，按需安装

# 向量数据库（长期记忆系统）
chromadb==1.3.2

# Transformers（OOC检测语义分析）
transformers==4.57.1
sentence-transformers==3.4.1

# 中文分词（OOC检测依赖）
jieba==0.42.1

# 可视化图表
pyecharts==2.0.0

# 统计分析（趋势检测、线性回归）
scipy==1.14.1

# 模糊字符串匹配（重复检测）
rapidfuzz==3.9.0

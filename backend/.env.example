# ==========================================
# WLai 配置文件示例
# ==========================================
# 复制此文件为 .env 并修改配置值
# cp .env.example .env

# ==========================================
# 应用配置
# ==========================================
APP_NAME=WLai
APP_VERSION=2.0.0
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=false
TZ=Asia/Shanghai

# ==========================================
# PostgreSQL 数据库配置
# ==========================================

# PostgreSQL 连接信息
POSTGRES_DB=wlai
POSTGRES_USER=wlai
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_PORT=5432

# 数据库连接 URL（Docker 部署时自动生成）
# DATABASE_URL=postgresql+asyncpg://wlai:change_me@localhost:5432/wlai_novel

# ==========================================
# SQLite 数据库配置
# ==========================================

# DATABASE_URL=sqlite+aiosqlite:///data/ai_story.db

# ==========================================
# 日志配置
# ==========================================
LOG_LEVEL=INFO
LOG_TO_FILE=true
LOG_FILE_PATH=logs/app.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=30

# ==========================================
# CORS 配置
# ==========================================
CORS_ORIGINS=["http://localhost:8000","http://127.0.0.1:8000"]

# ==========================================
# 代理配置（可选）
# ==========================================
# HTTP_PROXY=http://your-proxy:port
# HTTPS_PROXY=http://your-proxy:port
# NO_PROXY=localhost,127.0.0.1

# ==========================================
# AI 服务配置（至少配置一个）
# ==========================================

# OpenAI 配置
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# 默认 AI 配置
DEFAULT_AI_PROVIDER=openai
DEFAULT_MODEL=gpt-4o-mini
DEFAULT_TEMPERATURE=0.7
DEFAULT_MAX_TOKENS=32000

# ==========================================
# 本地账户登录配置
# ==========================================
LOCAL_AUTH_ENABLED=true
LOCAL_AUTH_USERNAME=admin
LOCAL_AUTH_PASSWORD=admin123
LOCAL_AUTH_DISPLAY_NAME=本地管理员

# ==========================================
# 会话配置
# ==========================================
SESSION_EXPIRE_MINUTES=120
SESSION_REFRESH_THRESHOLD_MINUTES=30
# 会话 Cookie 是否强制 Secure：留空按 DEBUG 自动判断；HTTP 部署可设为 false
# SESSION_COOKIE_SECURE=false

# ==========================================
# SMTP 默认配置（可在系统设置中被管理员覆盖）
# ==========================================
SMTP_PROVIDER=qq
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USERNAME=your-email@qq.com
SMTP_PASSWORD=your-qq-smtp-auth-code
SMTP_USE_TLS=false
SMTP_USE_SSL=true
SMTP_FROM_EMAIL=your-email@qq.com
SMTP_FROM_NAME=WLai
EMAIL_AUTH_ENABLED=true
EMAIL_REGISTER_ENABLED=true
EMAIL_VERIFICATION_CODE_TTL_MINUTES=10
EMAIL_VERIFICATION_RESEND_INTERVAL_SECONDS=60

# ==========================================
# 提示词工坊配置
# ==========================================
# 运行模式：client（本地部署）或 server（云端服务器）
WORKSHOP_MODE=client

# 云端服务地址（client 模式使用，留空则禁用）
# WORKSHOP_CLOUD_URL=

# 云端 API 请求超时时间（秒）
WORKSHOP_API_TIMEOUT=30

# ==========================================
# 向量模型配置
# ==========================================
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DIMENSION=1024
EMBEDDING_FALLBACK=moka-ai/m3e-base

# ==========================================
# 多模态模型配置
# ==========================================
# 后端选择: none(默认，禁用) | cloud(云端API) | local(本地GPU)
MULTIMODAL_BACKEND=none
# 云端配置（MULTIMODAL_BACKEND=cloud 时生效）
MULTIMODAL_CLOUD_PROVIDER=openai
MULTIMODAL_CLOUD_MODEL=gpt-4o
# 本地配置（MULTIMODAL_BACKEND=local 时生效，需 GPU）
# MULTIMODAL_LOCAL_MODEL=Qwen/Qwen2.5-VL-7B-Instruct
# MULTIMODAL_LOCAL_DEVICE=cuda

# ==========================================
# Docker 部署说明
# ==========================================
# Docker 部署时 DATABASE_URL 和 REDIS_URL 由 docker-compose 自动注入
# 无需手动配置，仅需确保 .env 中其他配置项正确即可
# REDIS_URL=redis://redis:6379/0
